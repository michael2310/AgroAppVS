@model AgroApp.Models.MachineServiceModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div class="col-sm col-md col-lg mx-auto">
    <div class="card border-0 shadow rounded-3 my-5">
        <div class="card-body p-4 p-sm-5">
            <div>
                <h4>Szczegóły przeglądu</h4>
                <hr />
                <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ServiceDate)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ServiceDate)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ServiceInfo)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ServiceInfo)
        </dd>
    </dl>
            </div>
            </div>
    </div>
</div>
<div>
    <input type="button" class="btn btn-primary btn-sm m-0" value="Edycja" onclick="location.href='@Url.Action("Edit", "MachineService", new{id = @Model.ServiceId})'" />
    <input type="button" class="btn btn-primary btn-sm m-0" value="Powrót do listy" onclick="location.href='@Url.Action("Details", "Machine", new {id = Model.MachineId})'" />
</div>
